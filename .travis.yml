language: python
python:
   - 2.7
   - 3.3
   - pypy
install:
   - pip install nose coverage
script:
  - nosetests --with-coverage --cover-package=okonomiyaki okonomiyaki
notifications:
  email:
    - davidc@enthought.com
